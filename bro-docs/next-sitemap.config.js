/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://brojs.yessindevs.me',
  generateRobotsTxt: true,
  exclude: ['*/_meta'], // Exclude Nextra specific meta files if needed
}
